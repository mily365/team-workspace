import { ToRenderDirective } from './to-render.directive';

describe('ToRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new ToRenderDirective();
    expect(directive).toBeTruthy();
  });
});
